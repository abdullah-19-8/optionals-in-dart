void main(List<String> args) {
  const String? name = null;

  // this ? is called the null-aware operator and make the code optional
  // if we do not use this ? mark this code will be error
  // const String name = null;
  print(name);
  print(null);
}
